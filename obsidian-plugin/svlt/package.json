{
  "name": "obsidian-svlt",
  "version": "0.1.21",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run test --exclude 'dist/**'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "npm run typecheck && node esbuild.config.mjs",
    "check:build-artifact": "test -f main.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2",
    "obsidian": "^1.8.7",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "zod": "^4.5.4"
  }
}
